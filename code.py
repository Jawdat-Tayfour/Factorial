import math
n = int(input())
print(math.factorial(n)) # that's a built-in function in the math library in python it returns the factorial of it's int argument, I know I shouldn't be posting a 3 line worth of code on github but idk maybe a beginner will need it one day.
